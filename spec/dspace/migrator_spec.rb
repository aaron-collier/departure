require "spec_helper"

describe Dspace::Migrator do
  it "has a version number" do
    expect(Dspace::Migrator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
